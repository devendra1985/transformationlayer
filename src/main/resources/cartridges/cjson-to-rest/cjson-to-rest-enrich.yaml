cartridgeId: cjson-to-rest

rules:
  # Add 3 additional fields
  - set:
      target: channel
      value: MOBILE
  - set:
      target: sourceSystem
      value: FEED
  - set:
      target: processedAt
      value: ${now}

  # Example enhancement using Java method (lookup / business logic):
  # calls bean method: exampleEnrichmentFunctions.bumpAmount(Map body) -> Map patch
  - call:
      bean: exampleEnrichmentFunctions
      method: bumpAmount

