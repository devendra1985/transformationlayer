# ============================================================================
# VISA VISABA EUR - Route Definition
# ============================================================================

route:
  id: VISABA-EUR-route
  from:
    uri: "direct:VISABA-EUR"
    steps:
      - bean:
          ref: persistRaw
          method: process
      - bean:
          ref: validate
          method: process
      - bean:
          ref: enrich
          method: process
      - bean:
          ref: transform
          method: process
      - bean:
          ref: persistTransformed
          method: process
      - setHeader:
          name: Content-Type
          constant: application/json
